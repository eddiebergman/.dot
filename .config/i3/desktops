# https://i3wm.org/docs/userguide.html
set $browser firefox
set $terminal kitty
set $reader evince


exec workspace 1; $terminal
exec workspace 2; $browser;
exec workspace 0; $terminal

exec workspace 1;
